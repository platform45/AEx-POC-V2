# Required Functionality 

Required basic functionality 

## User stories 

### 1. Queries
The user must be able to make a query, the backend may then need to generate an SQL query and apply that to the database e.g. "How many issues occured" 

The application must the perform this action and send back the required response and data to be displayed on the frontend 

## Tool use 
- Tool use and functionality like generating SQL queries is needed 